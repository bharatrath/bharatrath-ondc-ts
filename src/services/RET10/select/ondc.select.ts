import { FullOrderData } from "../../../RET10.Schema/select";

export class OndcSelectService {
  async select(orderData: FullOrderData) {
    // its shoudl in dattbase
    console.log("here you have to save all the datta in db");
  }
}
